## Features
- Multiple regex patterns for date, amount and merchant
- Date is normalised to ISO format
- Storage function which stores the transactions in a JSON
- Take multiple SMS as input from a file

## Issues
- Cannot distinguish between amount crediting and debiting 